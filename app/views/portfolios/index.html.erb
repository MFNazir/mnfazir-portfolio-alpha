<div class="album text-muted">
  <div class="container">
    <div class="row <%= "sortable" if logged_in?(:site_admin) %>">
        <%= render @portfolios %>
        
    </div>
    <% if logged_in?(:site_admin) %>
    <hr>
      <h3>Create A New Portfolio:</h3>
        <div class="">
          <%= link_to "Create A New Portfolio", new_portfolio_path(@portfolio), class: 'btn btn-primary' %>
        </div>
      <% end %>
  </div>
</div>