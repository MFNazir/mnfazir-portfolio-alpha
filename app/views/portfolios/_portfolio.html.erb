 <p><%= link_to portfolio.title, portfolio_show_path(portfolio) %></p>

    <p><%= portfolio.subtitle%></p>

    <p><%= portfolio.body %></p>

    <%= image_tag portfolio.thumb_image unless portfolio.thumb_image.nil? %>
    <%= link_to "Edit Item", edit_portfolio_path(portfolio) if logged_in?(:site_admin) %>
    <%= link_to 'Destroy', portfolio_path(portfolio), method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %>